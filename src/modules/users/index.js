// Importa el router principal del módulo de usuarios.
const usersRouter = require("./users.routes");

// Exporta el router para que la aplicación pueda montarlo en la ruta correspondiente.
module.exports = usersRouter;
