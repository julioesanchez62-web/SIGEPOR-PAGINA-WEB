/**
 * Módulo de Usuarios
 * 
 * Exporta las rutas del módulo para que sean registradas en src/routes/index.js
 * Punto de entrada único para este módulo.
 */

const usersRoutes = require('./users.routes');

module.exports = usersRoutes;
